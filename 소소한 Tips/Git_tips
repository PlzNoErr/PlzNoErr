git 명령어들

git clone 주소
git add .
git commit -m "~~~~"
git push origin master(main)
git pull
git fetch
git pull origin master
git checkout
git checkout user (branch 브랜치 전환)
git branch -b 이름 (새로운 브랜치 생성)
